{
  "name": "web-socket-api-app",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "lint": "eslint ./**/*.ts"
  },
  "dependencies": {
    "@aws-sdk/client-apigatewaymanagementapi": "^3.398.0",
    "@aws-sdk/client-dynamodb": "^3.370.0",
    "@aws-sdk/lib-dynamodb": "^3.370.0",
    "@aws-sdk/util-dynamodb": "^3.370.0",
    "aws-jwt-verify": "^4.0.0",
    "aws-lambda": "^1.0.7"
  }
}
